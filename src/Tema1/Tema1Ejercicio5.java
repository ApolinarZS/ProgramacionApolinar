package Tema1;

public class Tema1Ejercicio5 {

    public static void main(String[] args){

        //Calculamos el resultado de la primera operación
        int r1 = 24 % 5;
        //Mostramos el resultado por pantalla
        System.out.println(r1);

        //Calculamos el resultado de la segunda operación
        double r2 = 7 / 2 + 2.5;
        //Mostramos el resultado por pantalla
        System.out.println(r2);

        //Calculamos el resultado de la tercera operación
        double r3 = 10.8 / 2 + 2;
        //Mostramos el resultado por pantalla
        System.out.println(r3);

        //Calculamos el resultado de la cuarta operación
        double r4 = ( 4 + 6 ) * 3 + 2 * ( 5 - 1 );
        //Mostramos el resultado por pantalla
        System.out.println(r4);

        //Calculamos el resultado de la quinta operación
        double r5 = 5 / 2 + 17 % 3;
        //Mostramos el resultado por pantalla
        System.out.println(r5);

        //Calculamos el resultado de la sexto operación
        boolean r6 = 7 >= 5 || 27 != 8;
        //Mostramos el resultado por pantalla
        System.out.println(r6);

        //Calculamos el resultado de la séptima operación
        boolean r7 = (45<=7) || ! (5>=7);
        //Mostramos el resultado por pantalla
        System.out.println(r7);

        //Calculamos el resultado de la octava operación
        int r8 = 27%4+15/4;
        //Mostramos el resultado por pantalla
        System.out.println(r8);

        //Calculamos el resultado de la novena operación
        double r9 = 37 /4*4-2;
        //Mostramos el resultado por pantalla
        System.out.println(r9);

        //Calculamos el resultado de la decima operación
        boolean r10 = (25>=7) && ! (7<=2);
        //Mostramos el resultado por pantalla
        System.out.println(r10);

        //Calculamos el resultado de la onceava operación
        boolean r11 = ('H'< 'J') && ('9'!='7');
        //Mostramos el resultado por pantalla
        System.out.println(r11);

        //Calculamos el resultado de la doceava operación
        boolean r12 = 25>20 & 13>5;
        //Mostramos el resultado por pantalla
        System.out.println(r12);

        //Calculamos el resultado de la treceava operación
        boolean r13 = 10+4<15-3 || 2*5+1>14-2*2;
        //Mostramos el resultado por pantalla
        System.out.println(r13);

        //Calculamos el resultado de la catorceava operación
        boolean r14 = 4*2<=8 || 2*2<5 & 4>3+1;
        //Mostramos el resultado por pantalla
        System.out.println(r14);

        //Calculamos el resultado de la quinceava operación
        boolean r15 = 10<=2*5 & 3<4 || ! (8>7) & 3*2<=4*2-1;
        //Mostramos el resultado por pantalla
        System.out.println(r15);
    }
}
